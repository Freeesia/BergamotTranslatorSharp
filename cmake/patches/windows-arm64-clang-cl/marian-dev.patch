diff --git a/CMakeLists.txt b/CMakeLists.txt
index e22af91..0624b70 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -16,0 +17,6 @@ set(CMAKE_CXX_STANDARD_REQUIRED ON)
+set(MARIAN_WINDOWS_ARM64_CLANG_CL OFF)
+if(WIN32 AND MSVC AND CMAKE_CXX_COMPILER_ID MATCHES "Clang" AND
+   CMAKE_SYSTEM_PROCESSOR MATCHES "^(ARM64|arm64|aarch64)$")
+  set(MARIAN_WINDOWS_ARM64_CLANG_CL ON)
+endif()
+
@@ -98 +104 @@ if(USE_SIMD_UTILS)
-  if(MSVC)
+  if(MSVC AND NOT CMAKE_CXX_COMPILER_ID MATCHES "Clang")
@@ -100 +106 @@ if(USE_SIMD_UTILS)
-  else(MSVC)
+  elseif(NOT MSVC)
@@ -102 +108 @@ if(USE_SIMD_UTILS)
-  endif(MSVC)
+  endif()
@@ -134 +140 @@ if(NOT COMPILE_WASM)
-  if(MSVC)
+  if(MSVC AND NOT MARIAN_WINDOWS_ARM64_CLANG_CL)
@@ -149 +155,3 @@ if(NOT COMPILE_WASM)
-  endif(MSVC)
+  elseif(MSVC)
+    set(MSVC_BUILD_ARCH "")
+  endif()
@@ -222 +230,3 @@ if(MSVC)
-  add_definitions(-DUSE_SSE2=1)
+  if(NOT MARIAN_WINDOWS_ARM64_CLANG_CL)
+    add_definitions(-DUSE_SSE2=1)
+  endif()
@@ -229 +239,5 @@ if(MSVC)
-  set(CMAKE_CXX_FLAGS_RELEASE          "${CMAKE_CXX_FLAGS} /MT /O2 ${INTRINSICS} /MP /GL /DNDEBUG")
+  if(MARIAN_WINDOWS_ARM64_CLANG_CL)
+    set(CMAKE_CXX_FLAGS_RELEASE        "${CMAKE_CXX_FLAGS} /MT /O2 ${INTRINSICS} /DNDEBUG")
+  else()
+    set(CMAKE_CXX_FLAGS_RELEASE        "${CMAKE_CXX_FLAGS} /MT /O2 ${INTRINSICS} /MP /GL /DNDEBUG")
+  endif()
@@ -234 +248,5 @@ if(MSVC)
-  set(CMAKE_EXE_LINKER_FLAGS         "${CMAKE_EXE_LINKER_FLAGS} /DEBUG /LTCG:incremental /INCREMENTAL:NO /ignore:4049")
+  if(MARIAN_WINDOWS_ARM64_CLANG_CL)
+    set(CMAKE_EXE_LINKER_FLAGS       "${CMAKE_EXE_LINKER_FLAGS} /DEBUG /INCREMENTAL:NO /ignore:4049")
+  else()
+    set(CMAKE_EXE_LINKER_FLAGS       "${CMAKE_EXE_LINKER_FLAGS} /DEBUG /LTCG:incremental /INCREMENTAL:NO /ignore:4049")
+  endif()
@@ -237 +255,3 @@ if(MSVC)
-  set(CMAKE_STATIC_LINKER_FLAGS      "${CMAKE_STATIC_LINKER_FLAGS} /LTCG:incremental")
+  if(NOT MARIAN_WINDOWS_ARM64_CLANG_CL)
+    set(CMAKE_STATIC_LINKER_FLAGS    "${CMAKE_STATIC_LINKER_FLAGS} /LTCG:incremental")
+  endif()
